# echo-generator
MATLAB project implementing an audio echo generator using time-domain signal processing. The project applies delay and amplitude scaling to audio signals, handles signal length matching, and includes normalization to prevent clipping. Built as a practical DSP application.
